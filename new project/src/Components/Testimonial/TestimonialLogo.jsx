import React from "react";

export default function TestimonialLogo({src, alt}) {
    return (
        <div className="testimonial-logo">
            <img src={src} alt={alt}/>
        </div>
    ) 
}